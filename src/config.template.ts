export interface IConfig {
    watchers: string[];
    token: string;
    groupId: number;
}

const config: IConfig = {
    watchers: [],
    token: "",
    groupId: 0
};

export default config;
// Path: src/config.ts
